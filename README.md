# GamingSite-with-LDMode
A quick site build to practice the Javascript used to switch between a light mode and dark mode. Using querySelectors, onclick function and if statements to produce the logic to switch between the Light and dark mode as well as changing the textContent.
